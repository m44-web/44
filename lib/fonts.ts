// Font CSS variables are set directly via CSS since Google Fonts API
// may not be available in all build environments.
// Fonts are loaded via globals.css @import or system fallbacks.

export const fontVariableClasses = "";
